# Data Sources & Schema Reference

**Date**: 2-Apr-26
**Purpose**: Enumerates all external data sources DagNet can connect to, their contracts, and authentication. Catalogues canonical schema files and their relationships.

---

## Part 1: External Data Sources

DagNet connects to external sources via the **Data Adapter Service (DAS)**, a pluggable transformation pipeline defined in `connections.yaml`. Each adapter converts DagNet queries to provider-specific API requests and transforms responses back into parameter values.

### Amplitude Analytics

| | |
|---|---|
| **Provider ID** | `amplitude` |
| **Kind** | `http` (REST API) |
| **Data provided** | Conversion funnel rates (n/k), daily time-series, cohort breakdowns |
| **Auth** | `basic` — API Key + Secret Key combined into Basic Auth header (auto-generated by DASRunner) |
| **Base URL** | `https://amplitude.com/api/2` (US) or `https://analytics.eu.amplitude.com/api/2` (EU) |
| **Rate limits** | ~360 requests/hour (standard tier) |

**Query semantics**:
- **Window mode**: X-anchored (event date window). Aggregated counts for users encountering events within the date range.
- **Cohort mode**: A-anchored (cohort entry window). Returns data for users entering the funnel during the cohort window.
- Supports native `exclude()` via inline behavioural cohorts (segment filters).
- Supports `visited()` via ordered funnel steps.
- Maximum funnel length: 10 steps.
- Supports daily time-series breakdown (`i=1` parameter).
- Composite queries (`minus()`/`plus()`) used as fallback when provider doesn't support native exclusion.

**Configuration** (in `connections.yaml`):
```yaml
name: amplitude-prod
provider: amplitude
kind: http
enabled: true
credsRef: amplitude
defaults:
  base_url: "https://amplitude.com/api/2"
  app_id: "334050"
  org_id: "126433"
  excluded_cohorts: ["9z057h6i"]  # Global exclusions (e.g. test users)
```

### Google Sheets

| | |
|---|---|
| **Provider ID** | `google-sheets` |
| **Kind** | `http` (Google Sheets API v4) |
| **Data provided** | Static/semi-static parameter values (probabilities, sample sizes, costs) |
| **Auth** | `google-service-account` — Service Account JSON (base64-encoded). Auto-generates OAuth access tokens. |
| **Base URL** | `https://sheets.googleapis.com/v4` |
| **Rate limits** | ~300 requests/minute |

**Query semantics**:
- Not funnel-based. Treats spreadsheets as lookup tables.
- Adapter extracts values by A1-notation range.
- No native support for `exclude()`, `visited()`, or context segmentation.

**Configuration**:
```yaml
name: sheets-readonly
provider: google-sheets
kind: http
enabled: true
credsRef: google-sheets
connection_string_schema:
  properties:
    spreadsheet_id: { type: string }
    range: { type: string }  # e.g. "Sheet1!A1:B10"
```

### Statsig

| | |
|---|---|
| **Provider ID** | `statsig` |
| **Kind** | `http` (Console API) |
| **Data provided** | Experiment variant allocations, feature gate configurations |
| **Auth** | `api-key` — Console API Key |
| **Base URL** | `https://api.statsig.com` |

**Query semantics**:
- Maps DagNet `case()` filters to Statsig experiment/gate definitions.
- Returns variant allocation metadata, not conversion data.

### PostgreSQL (SQL)

| | |
|---|---|
| **Provider ID** | `postgres` |
| **Kind** | `sql` (native database connection) |
| **Data provided** | Conversion data, event counts, custom metrics via SQL |
| **Auth** | `basic` — host, port, database, username, password |

**Query semantics**:
- Adapter builds SQL queries from DagNet DSL.
- Supports native `exclude()` (via WHERE), `visited()` (via JOIN/subqueries), and ordered funnel logic.

---

## Credential Types

Stored in `credentials.yaml` and referenced by `credsRef` in connections.

| Auth Type | Fields | Auto-Processing |
|---|---|---|
| `api-key` | `api_key`, `secret_key` | Passed as-is |
| `basic` | `username`, `password` | Auto-generates `basic_auth_b64` header |
| `google-service-account` | `service_account_json_b64` | Auto-generates OAuth `access_token` |
| `oauth` | `access_token`, `refresh_token` | Caller must handle refresh |

---

## Fetch Modes

| Mode | Behaviour |
|---|---|
| **Direct** | Fetches data, applies to graph, no storage. For quick checks. |
| **Versioned** | Fetches data, stores daily time-series in parameter files and snapshot DB. Supports incremental fetch (only missing days) and cohort aggregation. Stores provenance metadata. |

---

## Query Compilation (MSMDC)

DagNet auto-generates optimal query strings via **Minimal Set of Maximally Discriminating Constraints** (see `PYTHON_BACKEND_ARCHITECTURE.md`):

1. Anchor: `from(edge.from).to(edge.to)`
2. Discriminate: find paths that don't use the direct edge
3. Add constraints (`visited()`/`exclude()`) until unique
4. Complexity management: avoids 2^k explosion via DAG reachability

For edges requiring dual n/k queries (upstream-conditioned paths), MSMDC generates separate `query` (for k) and `n_query` (for n) strings. See `FETCH_PLANNING_PRINCIPLES.md` for the fill-missing + refresh-stale contract.

---

## Part 2: Schema Files Catalogue

### JSON Schemas (`graph-editor/public/schemas/`)

| File | Defines | Version |
|---|---|---|
| `conversion-graph-1.0.0.json` | Legacy graph structure | v1.0 |
| `conversion-graph-1.1.0.json` | **Current** graph structure: nodes, edges, policies, metadata, postits, containers, canvasAnalyses, canvasViews, _bayes | v1.1 |
| `query-dsl-1.0.0.json` | Legacy DSL grammar | v1.0 |
| `query-dsl-1.1.0.json` | **Current** DSL grammar: all 14 functions (see `DSL_SYNTAX_REFERENCE.md`) | v1.1 |
| `connections-schema.json` | Connection definitions and DAS adapter pipeline | v1.0 |
| `credentials-schema.json` | Git + provider credential storage | v1.0 |
| `settings-schema.json` | Forecasting and statistical settings (recency half-life, min ESS, t95, blend lambda, Bayes priors) | v1.0 |

### YAML Schemas (`graph-editor/public/param-schemas/`)

| File | Defines |
|---|---|
| `parameter-schema.yaml` | Individual parameter: id, name, type, values, metadata, query, connection, latency config, posterior |
| `case-parameter-schema.yaml` | Case node parameter (variant weights) |
| `node-schema.yaml` | Node: id, name, entry flag, event_id, type, absorbing flag |
| `event-schema.yaml` | Event: id, name, provider_event_names, per-provider filters |
| `context-definition-schema.yaml` | Context dimension: key, values, MECE partitioning rules |
| `context-schema.yaml` | Context instance: key, value, description |
| `registry-schema.yaml` | Index file schema (for nodes-index, parameters-index, etc.) |
| `nodes-index-schema.yaml` | Nodes index entries |
| `parameters-index-schema.yaml` | Parameters index entries |
| `events-index-schema.yaml` | Events index entries |
| `contexts-index-schema.yaml` | Contexts index entries |
| `cases-index-schema.yaml` | Cases index entries |

### Type Generation Relationship

```
conversion-graph-1.1.0.json  (canonical source of truth)
        │
        ▼
  lib/graph_types.py   (Pydantic models — generated from JSON schema)
        │
        ▼
  Python backend types  (Evidence, Latency, SlicePosteriorEntry, QueryPayload, etc.)
```

TypeScript types in `src/types/index.ts` are maintained manually but track the same shapes.
