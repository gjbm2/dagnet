{
  "gates": {
    "git-write": {
      "description": "Commands that modify git state",
      "patterns": [
        "git add", "git commit", "git push", "git pull",
        "git checkout", "git reset", "git rebase", "git merge",
        "git stash", "git revert", "git tag", "git rm", "git mv",
        "git clean", "git restore", "git switch", "git cherry-pick",
        "git apply", "git am", "git filter-branch", "git config",
        "git branch ",
        "--force"
      ],
      "claude_md_section": "Gate 1: Git writes"
    },
    "package-install": {
      "description": "Commands that add or remove dependencies",
      "patterns": [
        "pip install", "pip uninstall",
        "npm install", "npm uninstall", "npm add", "npm remove",
        "npx add", "yarn add", "yarn remove", "pnpm add", "pnpm remove"
      ],
      "claude_md_section": "Gate 3: Package installs"
    },
    "destructive": {
      "description": "Commands that delete or overwrite data",
      "patterns": [
        "rm ", "rm\t", "rmdir ", "unlink ", "truncate ", "shred ", "sed -i",
        "cp ", "cp\t", "mv ", "mv\t", "tee ", "tee\t", "dd of=",
        "awk -i inplace", "awk --inplace",
        "dagnet-gate-"
      ],
      "claude_md_section": "Gate 2: Destructive / data-loss operations"
    }
  }
}
