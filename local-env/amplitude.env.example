# Local-only real Amplitude E2E tests
#
# Copy to: .env.amplitude.local (repo root) and fill in credentials.
# That file is gitignored.
#
# Usage:
# - Run the local-only test file under graph-editor (see its header for the command).

AMPLITUDE_API_KEY=
AMPLITUDE_SECRET_KEY=


