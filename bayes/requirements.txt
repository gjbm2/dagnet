# Bayes worker dependencies — single source of truth for dev venv AND Modal image.
# Both sides install from this file. If you add a package here, it goes everywhere.
# These must NEVER go into graph-editor/requirements.txt (Vercel size limit).
#
# Install into the shared venv:
#   . graph-editor/venv/bin/activate && pip install -r bayes/requirements.txt

numpy>=2.0
scipy>=1.14
pymc>=5.28
arviz>=0.23
nutpie>=0.16
jax[cpu]>=0.4.30  # nutpie jax backend (jax_backend feature flag)
requests>=2.32
pyyaml>=6.0
psycopg2-binary>=2.9
