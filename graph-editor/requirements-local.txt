# Local development dependencies — superset of production.
# Install with:  pip install -r requirements-local.txt
#
# Includes everything Vercel needs (via -r requirements.txt) plus the
# dev-server, test runner, and Modal CLI.

-r requirements.txt

# Dev server (dev-server.py uses FastAPI + Uvicorn)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-dotenv>=1.0.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0

# Modal CLI (Bayes deploys; Modal worker image uses bayes/requirements.txt)
modal>=1.0.0
