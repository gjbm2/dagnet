{
  "ui:order": ["id", "name", "type", "query", "query_overridden", "n_query", "n_query_overridden", "connection", "connection_string", "latency", "posterior", "force_replace_at_ms", "values", "metadata"],
  "ui:options": {
    "groups": [
      {
        "title": "Identity",
        "fields": ["id", "name", "type"],
        "defaultExpanded": true
      },
      {
        "title": "Query & Connection",
        "fields": ["query", "query_overridden", "n_query", "n_query_overridden", "connection", "connection_string"]
      },
      {
        "title": "Latency",
        "fields": ["latency"]
      },
      {
        "title": "Bayesian Posterior",
        "fields": ["posterior", "force_replace_at_ms"]
      }
    ]
  },
  "type": {
    "ui:widget": "select"
  },
  "query": {
    "ui:help": "DSL: from(node-id).to(node-id)"
  },
  "n_query": {
    "ui:help": "Optional: explicit n query when denominator differs from k query"
  },
  "connection": {
    "ui:help": "Connection from connections.yaml"
  },
  "connection_string": {
    "ui:widget": "MonacoWidget",
    "ui:options": {
      "language": "json",
      "height": "80px"
    },
    "ui:help": "Provider-specific JSON overrides"
  },
  "latency": {},
  "posterior": {
    "ui:field": "NullField"
  },
  "force_replace_at_ms": {
    "ui:field": "NullField"
  },
  "values": {
    "ui:field": "ValuesTableField"
  },
  "metadata": {
    "ui:options": {
      "accordion": true
    },
    "description": {
      "ui:widget": "textarea",
      "ui:options": {
        "rows": 2
      }
    },
    "status": {
      "ui:widget": "select"
    },
    "constraints": {
      "ui:options": {
        "accordion": true
      }
    },
    "aliases": {
      "ui:options": {
        "orderable": true
      }
    },
    "tags": {
      "ui:options": {
        "orderable": true
      }
    },
    "references": {
      "ui:options": {
        "orderable": true
      },
      "items": {
        "ui:options": {
          "accordion": true
        },
        "type": {
          "ui:widget": "select"
        }
      }
    }
  }
}
