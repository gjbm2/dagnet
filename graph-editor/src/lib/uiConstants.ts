/**
 * Shared UI constants for layout and panels
 * 
 * Centralized to avoid duplication and ensure consistency across:
 * - graphSidebarLayout.ts
 * - useSidebarState.ts
 * - GraphEditor.tsx
 */

// ===== SIDEBAR =====

/** Default sidebar panel width (pixels) */
export const DEFAULT_SIDEBAR_WIDTH = 320;

/** Minimum sidebar width before it's considered collapsed (pixels) */
export const MIN_SIDEBAR_WIDTH = 50;

