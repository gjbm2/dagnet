/**
 * Edge display constants
 *
 * These are **UI-only** formatting rules for how values are rendered on the graph
 * (edge beads, etc.). They should not affect storage precision.
 */

/**
 * Probability mean shown on the graph as a percentage (0 d.p).
 * Example: `0.789` → `79%`
 */
export const EDGE_PROBABILITY_PERCENT_DECIMAL_PLACES = 0;

/**
 * Probability stdev shown on the graph as a ± percentage (0 d.p).
 * Example: `0.052` → `± 5%`
 */
export const EDGE_PROBABILITY_STDEV_PERCENT_DECIMAL_PLACES = 0;

/**
 * Completeness shown on the graph as a percentage (0 d.p).
 * Example: `0.789` → `79%`
 */
export const EDGE_COMPLETENESS_PERCENT_DECIMAL_PLACES = 0;

/**
 * Median lag shown on the graph in days (1 d.p).
 * Example: `6.44` → `6.4d`
 */
export const EDGE_MEDIAN_LAG_DAYS_DECIMAL_PLACES = 1;




